let cat = "cat";
let bat = "bat";
let mat = "mat";

//using classes for range alphabets.
let regex = /[a-b]at/;

console.log(cat.match(regex));
console.log(bat.match(regex));
console.log(mat.match(regex));
