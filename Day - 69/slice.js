const weather = ["sunny", "cold", "humid", "snow"];

const todayWeather = weather.slice(1,2);  //it doesnot returns last ones

console.log(todayWeather.toString());  //type conversion is very useful(If Sometimes,).

console.log(weather.toString())